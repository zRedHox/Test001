# Test001
test for applying jobs at เงินติดล้อ
ในส่วนนี้จะเป็น Part 1 จากโจทย์
	•	API Development (Flask / FastAPI / Django)
	•	POST /users → add new user (username, email)
	•	GET /users → list all users
	•	Use SQLite / PostgreSQL / SQL Server
	•	Return JSON
